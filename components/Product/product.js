import React from 'react'
import { View, Image, Text } from 'react-native'

import styles from './product.styles'

const Product = ({ date, commentaire, avatar}) => (
<View style = { styles.App } >

</View>
)

export default Product